<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace:编写接口的全类名，就是告诉要实现该配置文件是哪个接口的具体实现-->
<mapper namespace="com.liusiqi.mapper.StudentMapper">
    <select id="selectById" resultType="com.liusiqi.mode.Student">
        select * from student where id_ = #{id}
    </select>
    <select id="selectAllStudent" resultType="com.liusiqi.mode.Student">
        select * from student where delflag_ = 'N'
    </select>
    <!--增删改操作不需要返回值，增删改返回的是影响的行数，mybatis会自动做判断-->
    <insert id="insertStudent">
        INSERT INTO student VALUES (#{id}, #{name}, #{age}, #{classId}, #{delflag}, #{createdById}, #{createdTime}, #{modifiedById}, #{modifiedTime})
    </insert>
    <update id="updateStudent">
        update student set name_=#{name},age_=#{age},class_id_=#{classId} where id_ = #{id}
    </update>
    <delete id="deleteStudent">
        delete from student where id_ = #{id}
    </delete>
</mapper>